<UserControl x:Class="Dragonfly.Views.RepaymentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="32,26,32,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="0,0,0,22">
            <Button x:Name="AddBtn" DockPanel.Dock="Right" Style="{StaticResource BtnPrimary}" Content="+ Add debt" VerticalAlignment="Center" Click="Add_Click"/>
            <StackPanel>
                <TextBlock Text="Monthly Repayment" Style="{StaticResource H1}"/>
                <TextBlock Text="Interest-bearing debts — payoff timelines and what it takes to hit a goal." Style="{StaticResource Sub}" Margin="0,3,0,0"/>
            </StackPanel>
        </DockPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,20,24">
            <StackPanel x:Name="Body"/>
        </ScrollViewer>
    </Grid>
</UserControl>
